server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #nacos-server地址

# 这里是作为服务提供者暴露出去的内容
management:
  endpoints:
    web:
      exposure:
        include: '*'
