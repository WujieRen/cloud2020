server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
#        server-addr: n1.com.rwj:5555

management:
  endpoints:
    web:
      exposure:
        include: '*'